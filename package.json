{
  "name": "sufficit-mobile-ai-models",
  "version": "0.1.0",
  "description": "OpenAI-compatible embeddings API backed by a llama.cpp server running on an idle Android phone (Qwen3-Embedding-4B)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
